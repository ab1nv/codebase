#include <bits/stdc++.h>
using namespace std;

#define str string
#define ll long long
#define all(x) (x).begin(), (x).end()
#define f(i, c, n) for (i = (c); i < (n); i++)

int main() {
  int t;
  cin >> t;

  while (t--) {
    int n, k;
    cin >> n >> k;
  }
}
